# litellm 1.83.x declares pydantic==2.12.5; a higher minimum breaks the resolver.
pydantic==2.12.5
python-dotenv>=1.0
openai>=2.0
anthropic>=0.30
google-genai>=1.0
pyyaml>=6.0
types-PyYAML>=6.0  # stub package so mypy can type-check `import yaml`
pytest>=8.0
pdfplumber>=0.11.9
pikepdf>=10.5.1
sqlmodel>=0.0.22
fastapi>=0.136.1
uvicorn[standard]>=0.34
# litellm 1.83.x pins httpx==0.28.1; a bare >=0.27 lets pip pick 0.28.0 and the solver fails.
httpx==0.28.1
requests>=2.31
urllib3>=2.7.0  # patched transitive (pip-audit / OSV); bounds CVE fixes on urllib3
# Goals architecture — IMF CPI via SDMX (inflation_service); pulls pandas via sdmx1
sdmx1>=1.0
python-multipart>=0.0.27

# Phase 4 — Gmail API email scraper
google-api-python-client>=2.194.0
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.3.1
beautifulsoup4>=4.12
apscheduler>=3.11.2

# Auth — password hashing and signed session tokens
bcrypt>=4.0
itsdangerous>=2.2.0

# CI tooling — lint, type-checking, coverage
ruff>=0.4
mypy>=1.10
pytest-cov>=5.0

# Phase A.0 — asset foundation (holdings, prices, returns)
# yfinance: optional international marks only (e.g. GC=F); Indian equities use NSE bhav.
yfinance>=0.2
nse>=2.1
cryptography>=47.0.0
pyxirr>=0.10
scipy>=1.14

# Arth conversational agent (Sub-Plan 1)
litellm>=1.83.7
